# Kaarobar Desktop POS

Electron terminal for branch cashiers.

**Flow:** Landing/Sign-in → Owner dashboard → POS till.

```bash
npm install
npm start
```

Requires `kaarobar-BE` at `http://localhost:4000/api/v1`.

Theme matches web/mobile Deep Sapphire (`#1d4ed8` / sidebar `#0b1220`).
