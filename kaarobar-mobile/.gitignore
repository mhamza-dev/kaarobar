# Learn more https://docs.expo.dev/guides/monorepos/#ignore-native-directories

# Native projects are generated by `expo prebuild` (Continuous Native Generation).
# Configure them from app.json / config plugins, never by editing them in place.
/android
/ios

# Expo
.expo/
dist/
web-build/
expo-env.d.ts.bak

# Metro
.metro-health-check*

# node
node_modules/
npm-debug.log
yarn-error.log

# macOS
.DS_Store

# Bundle artifacts
*.jsbundle

# Signing — keep only the shared debug keystore under credentials/
*.keystore
!credentials/debug.keystore
*.p8
*.p12
*.key
*.mobileprovision

# testing
/coverage

# local env
.env
.env.local
.env*.local
