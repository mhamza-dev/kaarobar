# Copy to .env.local and adjust.

# The Phoenix backend's API base URL, including /api/v1.
# Local dev, backend run via `mix phx.server` or docker compose:
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
