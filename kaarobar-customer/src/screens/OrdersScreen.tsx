import React from "react";
import BuyerOrders from "../components/BuyerOrders";

/** Orders + appointments tab — detail screens are pushed from the list. */
export default function OrdersScreen() {
  return <BuyerOrders />;
}
